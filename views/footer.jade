.module#footer
  .leftguy
    .content
      &nbsp;
  .rightguy
    .content
      &nbsp;

script(type="text/javascript").
   var noop = function() {};
   function getScript(url,success) {
     var script = document.createElement('script');
     script.src = url;
     var head = document.getElementsByTagName('head')[0];
     var done = false;
     script.onload = script.onreadystatechange = function() {
       if (!done && (!this.readyState || this.readyState == 'loaded' || this.readyState == 'complete')) {
         done = true;
         success();
         script.onload = script.onreadystatechange = null;
         head.removeChild(script);
       }
     };
     head.appendChild(script);
   }
   getScript('/javascripts/jquery/dist/jquery.min.js',function() {
     getScript('/javascripts/jquery-lifestream/jquery.lifestream.min.js',function() {
       getScript('/javascripts/jquery-timeago/jquery.timeago.min.js',function() {
         getScript('/javascripts/lifestream.min.js',noop);
       });
     });
     getScript('/javascripts/colorgrad.min.js',noop);
   });
